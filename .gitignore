# Secrets / Env — NIE committen (rules/AGENT.md: harte Regel)
.env
*.env
!*.env.example
infra/gitea/.env

# Claude Code — lokale/persoenliche Overrides (committed wird nur settings.json + hooks)
.claude/settings.local.json

# Abhaengigkeiten / Build
node_modules/
dist/
build/
__pycache__/
*.pyc

# Editor / OS
.DS_Store
.idea/
